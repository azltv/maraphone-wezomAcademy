GIT

## Проверка состояния

	git status

## Добавление файлов в индекс отслеживания.

	git add <имя-файла>
	git add . (добавляет только новые файлы)
	git add --all (добавляет все изменения включая удаление файлов)

## Коммит изменений (локально)

	git commit -m "комментарий" 

## Привязка к удалённом репозиторию (один раз)

	git remote add origin https://github.com/antonzltv/maraphone-wezomAcademy.git

## Отправка изменений на сервер (гитхаб) с созданием ветки master

	git push -u origin master (один раз)
	git push

## Подтянуть изменения с сервера

	git pull

## Клонирование репозитория

	git clone <url-репозитория> <имя-папки-в-которую-подтянется-репозиторий>

## генерация ключа (генерятся они в домашней директории в скрытой папке .ssh)

	ssh-keygen

## Переход на коммит или ветку 

	git checkout <id-commit | name-of-branch>

## Создание ветки (шаблон указан ниже)

	git branch 76543-azltv-issueDescriptiom